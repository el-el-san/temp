```diff
diff --git a/README.md b/README.md
index 4c0d312..e8a1f7b 100644
--- a/README.md
+++ b/README.md
@@ -6,7 +6,7 @@ サンプルリポジトリのREADMEです。
-<!-- ここに現状時刻を追記したい -->
+<!-- TIME_MARK -->

 # 概要

@@ -15,3 +15,11 @@ 以下の手順でプロジェクトをセットアップしてください。
 
 ```
 ```
+
+### 時刻の更新方法
+
+```bash
+# (1) スクリプトに実行権限を付与（初回のみ）
+chmod +x scripts/update_readme_time.sh
+
+# (2) README.md の時刻を更新
+./scripts/update_readme_time.sh
+
+# (3) 変更をコミット＆プッシュ
+git add README.md
+git commit -m "Update README timestamp"
+git push
+```
```
```

```diff
diff --git a/scripts/update_readme_time.sh b/scripts/update_readme_time.sh
new file mode 100755
index 0000000..e9fa30d
--- /dev/null
+++ b/scripts/update_readme_time.sh
@@ -0,0 +1,23 @@
+#!/usr/bin/env bash
+set -euo pipefail
+
+# 現在時刻を取得（YYYY-MM-DD HH:MM:SS 形式）
+now=$(date '+%Y-%m-%d %H:%M:%S')
+
+# sed の -i オプションをプラットフォームに合わせて切り替え
+# GNU/Linux: sed -i, macOS: sed -i ''
+if sed --version >/dev/null 2>&1; then
+  SED_OPTS=(-i)
+else
+  SED_OPTS=(-i '')
+fi
+
+# プレースホルダーを置換
+sed "${SED_OPTS[@]}" \
+  -e "s/<!-- TIME_MARK -->/${now}/" \
+  README.md
+
+echo "README.md を ${now} で更新しました。"
```

```diff
diff --git a/.github/workflows/update-readme-time.yml b/.github/workflows/update-readme-time.yml
new file mode 100644
index 0000000..5cf6d4a
--- /dev/null
+++ b/.github/workflows/update-readme-time.yml
@@ -0,0 +1,22 @@
+name: Update README Time
+
+on:
+  schedule:
+    - cron: '0 * * * *'    # 毎時 0 分に実行
+  workflow_dispatch:      # 手動トリガーも可能
+
+jobs:
+  update-readme-time:
+    runs-on: ubuntu-latest
+    steps:
+      - uses: actions/checkout@v3
+      - name: Update README timestamp
+        run: |
+          chmod +x scripts/update_readme_time.sh
+          ./scripts/update_readme_time.sh
+      - name: Commit & Push if changed
+        uses: stefanzweifel/git-auto-commit-action@v4
+        with:
+          commit_message: "ci: update README timestamp"
```
```
---

✅ `scripts/update_readme_time.sh` に実行権限を付与済みです。

以上のとおり、README.md の TIME_MARK マーカー設置、スクリプトの追加・実行権限設定、実行手順のドキュメント追記、CI ワークフロー新規作成を行いました。ご確認ください。